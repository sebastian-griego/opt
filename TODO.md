# TODO

Keep items tiny and closed loop:

“Add FFT based convolution to eval, verify matches slow eval for m=50”

“Add hillclimb operator: k swap preserving measure constraint, log improvements”

“Add exact arithmetic check for piecewise constant candidates”
