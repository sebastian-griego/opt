# TODO

Keep items tiny and closed loop:

1. Implement slow eval for C1b + tests (constant function test, random candidate invariants)
2. Implement candidate format + save/load + mass projection
3. Implement baseline search + always append to results.csv + best_candidate updates
4. Add FFT evaluator + cross-check tests (slow vs FFT at m=30/40/50)
5. Add k-swap hillclimb operator (log improvements)
6. Add exact arithmetic or rational-check pathway
